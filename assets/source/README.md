# Source atlas

Drop the overworld map here as **`world_map.png`**.

This is the input for `tools/asset-pipeline/slice_map.py` (region/office reference crops +
grid-overlay preview) and the style reference for img2img / IP-Adapter generation. Everything
in `regions/` and `offices/` is generated from it.
